{
  "name": "bash-alias-app",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "echo \"Error: no test specified\" && exit 1",
    "win_build": "electron-packager . bashalias-windows=win32 --arch=x64 --overwrite --icon=icons/logo.png --out=release-builds",
    "ios_build": "electron-packager . bashalias-mac=darwin --arch=x64 --overwrite --icon=icons/logo.icns --out=release-builds"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "electron": "^31.1.0"
  },
  "devDependencies": {
    "@electron/packager": "^18.3.3"
  }
}
